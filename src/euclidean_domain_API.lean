import algebra.euclidean_domain

-- Non-trivial commutative ring with quotient and remainder

-- quotient : R → R → R
-- remainder : R → R → R

-- b * quotient(a b) + remainder(a b) = a
--a = quo * b + rem

-- if b ≠ 0, function r (a % b) b  True (mod_lt)


-- gcd non-unique: (gcd a b) | a and (gcd a b) | b and if c | a and c | b the c | (gcd a b)

-- euclidean_domain.xgcd_aux Extended Euclidean Algorithm

-- Bezouts Lemma `euclidean_domain.gcd_eq_gcd_ab`



-- # What Lemmas Do I Want?
-- 1x1 matrix equiv (is!) to smith normal form matrix
-- (n-1)x